package com.example.surveyapplication.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material.Shapes
import androidx.compose.ui.unit.dp

val Shapes = Shapes(
    small = RoundedCornerShape(8.dp),   // Slightly rounded for smaller components
    medium = RoundedCornerShape(16.dp), // More rounded for medium-sized components
    large = RoundedCornerShape(24.dp)   // A bold curve for larger components
)
